#!/usr/bin/env python3
"""Command-line entry point for Azure Kubernetes Credential Loader."""

import sys
import os

# Add src to path for imports when running as script
if __name__ == "__main__":
    sys.path.insert(0, os.path.join(os.path.dirname(__file__), "src"))
    from aks_credential_loader import main
    main()
